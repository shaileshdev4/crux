# Optional: enables Groq-powered extraction and insight narration.
# The app works without it via graceful fallbacks, but real extraction
# quality needs this set. Get a key at https://console.groq.com/keys
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
GROQ_EXTRACT_MODEL=llama-3.1-8b-instant
